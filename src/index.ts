/**
 * Public entry point for `misina` — a driver-based, cross-runtime
 * TypeScript HTTP client inspired by `unjs/unstorage` and `unemail`.
 *
 * @module
 */
export { createMisina } from "./misina.ts"
export type { Misina, MisinaOptions, MisinaRequest, MisinaResponse } from "./types.ts"
